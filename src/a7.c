#include "არადანი.h"
#include <stdio.h>
#include <stdlib.h>
Nar(ერთი,    მიადგი(int, 1) მიადგი(n_t, O(1)))
Nar(შეკრიბე, მოხსენი(int, r); მოხსენი(int, l); მიადგი(int, l + r) მიადგი(n_t, O(1)))
Nar(ორი,     მიადგი5(n_t, ერთი, ერთი, და, შეკრიბე, და))
Nar(შვიდი,   მიადგი13(n_t, ერთი, ორი, და, ორი, და, ორი, და,
                           შეკრიბე, და, შეკრიბე, და, შეკრიბე, და))
N(logint)    { მოხსენი(int, v); printf("%d\n", v); }
Main(        4096, malloc, free,
             მიადგი3(n_t, logint, logint, logint) ჩჩჩაახვიე მიადგი(n_t, შვიდი) C)
