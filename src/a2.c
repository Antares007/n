#include "aradani.h"
#include <stdio.h>
N(talloc);
N(open2) {}
N(open1) {
  printf("a\n");
  R(void *, o);
  void *s = abo;
  // oars
  open2(o, o, s, s);
}
N(open) { A(unsigned long, 64) A3(n_t, talloc, open1, da) C; }
N(shemdeg) {}
N(r0) { printf("r0\n"); }
N(r1) { printf("r1\n"); }
N(r2) { printf("r2\n"); }
#include <stdlib.h>
// 
// da
// chaakhvie .[3] chaakhvie (moakhie3 amoakhvie) chaakhvie (moakhie4 .[0]) chaakhvie.
// an
// chaakhvie .[3] chaakhvie (moakhie4 .[1]) chaakhvie (moakhie3 amoakhvie) chaakhvie.
// 
// ostream
// "r" 'OPEN' .[1] .[2].
// main
// "src/file.c" "r" 'OPEN' .[1] split transform 
// "src/file.c" "r" 'OPEN' .[1] split transform merge6
// "src/file.c" "r" 'OPEN' .[1] split transform merge6
// "src/file.c" "r" 'OPEN' .[1] split transform merge6.
Main(     (1 << 12), malloc, free,
          A3(n_t, r0, r1, r2) PPP
          A2(char *, "src/file.c", "r") A3(n_t, open, shemdeg, da) C
)
