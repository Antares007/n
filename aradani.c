#include "aradani.h"

S(dddu) { D3() U() Dot(); }
S(dddd0) { D4() C(, 0); }
N(da) {
  n_t ararkhi = ο[ρ + 2];
  CH() P3(, dddd0, dddu, ararkhi) Dot();
}
S(dddd1) { D4() C(, 1); }
N(an) {
  n_t ararkhi = ο[ρ + 2];
  CH() P3(, dddu, dddd1, ararkhi) Dot();
}
S(dddddd0) { D6() C(, 0); }
S(ddduuu) { D3() U3() Dot(); }
N(da3) {
  n_t araarkhi = ο[ρ + 2];
  CH3() P3(, dddddd0, ddduuu, araarkhi) Dot();
}
